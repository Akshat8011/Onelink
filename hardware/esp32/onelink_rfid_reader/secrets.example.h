/*
 * Optional overrides — copy to secrets.h and #include it if you need a
 * private broker later. Default firmware uses broker.emqx.io with no secrets.
 *
 * NEVER put a Raspberry Pi LAN IP here. Taps go through the public MQTT broker.
 */
// #define MQTT_HOST_OVERRIDE "broker.emqx.io"
// #define MQTT_PORT_OVERRIDE 1883
