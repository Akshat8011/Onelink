# OneLink ESP32 RFID reader

Arduino sketch: WiFiManager + PubSubClient → `broker.emqx.io`.

See [../../README.md](../../README.md) for flash steps, pin map, and portal usage.

Optional: copy `secrets.example.h` → `secrets.h` only if you later override the broker host.
**Never store a Raspberry Pi IP here.**
